{% extends 'base_template.html.twig' %}

{% block content %}
<div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="card shadow p-4 text-center">
        <h1 class="mb-4">Êtes-vous un robot ?</h1>
        <div class="d-flex justify-content-center gap-3 position-relative">
            <!-- Bouton Oui -->
            <button class="btn btn-danger btn-lg">Oui</button>
            
            <!-- Conteneur pour le bouton avec l'image -->
            <div class="position-relative" id="btnContainer">
                <img src="img/toHideLogo" id="hiddenLogo" alt="Logo"
                     class="position-absolute start-0 top-0 w-100 h-100" style="z-index: 0; object-fit: cover; border-radius: 5px;">
                <button class="btn btn-success btn-lg position-relative" id="btnOfficieux" style="z-index: 1;">Non</button>
            </div>

            <!-- Bouton Non officiel -->
            <button class="btn btn-success btn-lg d-none" id="btnOfficiel">Non</button>
        </div>
    </div>
</div>
{% endblock %}

{% block script %}
    <script src="js/captcha.js"></script>
{% endblock %}